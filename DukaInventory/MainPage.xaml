<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DukaInventory.MainPage"
             Title="Inventory">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add" Clicked="OnAddClicked" />
        <ToolbarItem Text="Sales" Clicked="OnSalesClicked" Order="Secondary" />
        <ToolbarItem Text="Export CSV" Clicked="OnExportClicked" Order="Secondary" />
    </ContentPage.ToolbarItems>

    <ScrollView>
        <VerticalStackLayout Padding="30,0" Spacing="25">
            
            <!-- Image -->
            <Image Source="dotnet_bot.png"
                   HeightRequest="185"
                   Aspect="AspectFit"
                   SemanticProperties.Description="dot net bot in a hovercraft number nine" />

            <!-- Headline -->
            <Label Text="Hello, World!"
                   Style="{StaticResource Headline}"
                   SemanticProperties.HeadingLevel="Level1" />

            <!-- SubHeadline -->
            <Label Text="Welcome to &#10;.NET Multi-platform App UI"
                   Style="{StaticResource SubHeadline}"
                   SemanticProperties.HeadingLevel="Level2"
                   SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <!-- Counter Button -->
            <Button x:Name="CounterBtn"
                    Text="Click me"
                    SemanticProperties.Hint="Counts the number of times you click"
                    Clicked="OnCounterClicked"
                    HorizontalOptions="Fill" />

            <!-- Inventory Section -->
            <StackLayout Padding="12">
                <Label Text="Your Items" FontSize="20" />
                
                <CollectionView x:Name="ItemsCollection"
                                SelectionMode="Single"
                                SelectionChanged="OnItemSelected">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Frame Padding="8" Margin="6" CornerRadius="8" HasShadow="True">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="80" />
                                        <ColumnDefinition Width="80" />
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Item Details -->
                                    <StackLayout>
                                        <Label Text="{Binding Name}" FontAttributes="Bold" />
                                        <Label Text="{Binding SKU}" FontSize="12"/>
                                        <Label Text="{Binding Quantity, StringFormat='Qty: {0}'}" FontSize="12"/>
                                    </StackLayout>
                                    
                                    <!-- Price -->
                                    <Label Grid.Column="1"
                                           Text="{Binding SellingPrice, StringFormat='Ksh {0:F2}'}"
                                           VerticalOptions="Center"
                                           HorizontalOptions="Center"/>
                                    
                                    <!-- Sell Button -->
                                    <Button Grid.Column="2"
                                            Text="Sell"
                                            Clicked="OnSellClicked"
                                            CommandParameter="{Binding Id}" />
                                </Grid>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <!-- Low Stock Warning -->
                <Label x:Name="LowStockLabel"
                       TextColor="Crimson"
                       IsVisible="False"
                       Margin="6"/>
            </StackLayout>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
